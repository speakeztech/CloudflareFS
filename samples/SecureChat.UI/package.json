{
  "name": "securechat-ui",
  "version": "1.0.0",
  "description": "SecureChat frontend with React and Tailwind",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "fable": "dotnet fable watch src --outDir src/.fable --run npm run dev",
    "fable:build": "dotnet fable src --outDir src/.fable",
    "deploy": "npm run fable:build && npm run build && npx wrangler pages deploy dist"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.1",
    "autoprefixer": "^10.4.16",
    "postcss": "^8.4.32",
    "tailwindcss": "^3.4.0",
    "vite": "^5.0.10"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}